# Branches whose mistral-specific commits are replayed on top of upstream/main
# to rebuild the `mistral-main` branch (see MISTRAL_README.md).
#
# The v1.0.1-prod feature branches were prepared against
# e380424c8bf8463b6f0d066095bd5bc81e03a9a3. Branches are replayed in this order.

mistral-branches                         # this branch: the rebuild tool, branch list, and CI workflow
lfm/v1.0.1-prod-epp-readiness             # plugin-provided EPP readiness
lfm/v1.0.1-prod-safe-disaggset-after-readiness  # safe DisaggregatedSet coordination; stacked on readiness
lfm/v1.0.1-prod-cross-replica-ticker-after-safe-disaggset  # independent publish loop; stacked on DisaggregatedSet
lfm/v1.0.1-prod-coordinator-revision-sync  # revision forwarding across coordinator phases
lfm/v1.0.1-prod-rawbody-release            # release request RawBody after Envoy forwarding
lfm/v1.0.1-prod-scoped-ttls                # per-priority and per-request queue TTLs
lfm/v1.0.1-prod-objective-key              # Eliot's inference-objective attribute support
lfm/v1.0.1-prod-multi-dp                   # shared-port multi-DP routing and KV events
lfm/v1.0.1-prod-kvevents-memory-fix-after-multi-dp  # retirement cleanup; stacked on multi-DP
