# Branches whose mistral-specific commits are replayed on top of upstream/main
# to rebuild the `mistral-main` branch (see scripts/update-mistral-main.py).
#
# Format:
#   - one branch name per line, resolved against the `origin` remote
#   - order matters: branches are applied top to bottom
#   - `#` starts a comment, either on its own line or after a branch name
#   - surrounding whitespace is ignored
#
# Each branch should be a small feature branch based directly on `main`. Ideally
# its comment links the upstream PR that carries the same change, so once that PR
# merges the branch can be dropped from this list. See MISTRAL_README.md.
#
# Commits already present upstream (by patch id) or already applied by an
# earlier branch in this list are skipped automatically.

mistral-branches                  # this branch: the rebuild script, this list and the CI workflow
lfm/combined-fixes                # cross-replica sync decouple, RawBody release, drop unused publisher const (no upstream PR yet)
lfm/dp-aware-routing-v2           # shared-port data-parallel routing and rank autodiscovery
mk/260819-routing-metrics         # 2 custom routing metrics (https://github.com/llm-d/llm-d-router/pull/2494)
mk/260821-no-fairness-metric      # allows to reduce the cardinality of fairness metrics (https://github.com/llm-d/llm-d-router/pull/2495)
